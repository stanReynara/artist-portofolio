services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nextjs-test
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
    restart: unless-stopped
